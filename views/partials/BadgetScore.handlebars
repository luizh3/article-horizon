<div
  class="flex shadow-md p-3 text-primary bg-white rounded font-bold space-x-5 w-56 justify-center items-center"
>
  <label class="text-xs xl:text-sm">{{title}}</label>
  <span
    class="
    {{#if ( isBiggerOrEqual score good)}}
    bg-green100 text-green800
    {{else}}
        {{#if (isBiggerOrEqual score medium)}}
         bg-yellow100 text-yellow800
        {{else}}
         bg-red100 text-red800 
        {{/if}}
    {{/if}} text-sm font-medium me-2 px-2.5 py-0.5 rounded dark:bg-red-900 dark:text-red-300"
  >{{score}}</span>
</div>